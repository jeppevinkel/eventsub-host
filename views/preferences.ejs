<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('partials/head'); %>
</head>
<body class="bg-secondary text-white">
    <header>
        <%- include('partials/header'); %>
    </header>

    <main class="container">
        <div class="mt-0 p-5">
            <h1>Preferences</h1>
        </div>

        <form>
            <div class="form-check form-switch">
                <input class="form-check-input" type="checkbox" role="switch" id="followEventSwitch">
                <label class="form-check-label" for="followEventSwitch">Follow event</label>
            </div>
            <div class="form-check form-switch">
                <input class="form-check-input" type="checkbox" role="switch" id="channelPointsRedeemRewardEventSwitch">
                <label class="form-check-label" for="channelPointsRedeemRewardEventSwitch">Channel point redeem reward event</label>
            </div>
        </form>

        <div class="">
            <h5 class="form-label">API Token</h5>
            <p>Used to access the websocket connection. Store the token a safe place after creation.</p>
            <button type="button" class="btn btn-dark" data-bs-toggle="tooltip" data-bs-placement="top" title="This will invalidate the old!">
                New Token
            </button>
        </div>
    </main>

    <%- include('partials/scripts'); %>
</body>
</html>